# PR 9 — prompt для агента: frontend cleanup

Ты работаешь в репозитории `opl-app`.

## Важно

Агент не запускает Docker/контейнеры. Команды для Docker только написать пользователю.

## Цель

Аккуратно вынести повторяемую frontend-логику в hooks/components.

## Ограничения

- Не меняй визуальный дизайн.
- Не переписывай весь `OplList.jsx`.
- Не добавляй новые библиотеки.
- Не запускай контейнеры.

## Первый шаг

1. Создай `frontend/src/hooks/useUndoDelete.js`.
2. Перенеси туда optimistic delete логику из `OplList.jsx`.
3. `OplList.jsx` должен использовать hook.
4. Поведение delete/undo должно остаться прежним.

## Второй шаг

Только если первый безопасен:

1. Создай `frontend/src/hooks/useOplFilters.js`.
2. Перенеси туда состояние поиска/сортировки/пагинации, если это можно сделать без большого риска.
3. Если риск высокий — остановись после `useUndoDelete`.

## Что агент может проверить локально

```bash
cd frontend
npm run build
```

Если build недоступен — честно указать.

## Ручная проверка для пользователя

- delete/undo работает;
- смена фильтра не отменяет pending delete;
- список OPL грузится как раньше.
